# AGENTS.md — NRA-IDE Agent Operating Contract

## 1. 位置付け

この文書は、NRA-IDE リポジトリで作業するAIエージェントの共通作業契約である。

この文書は、作業を不要に停止させるためではなく、対象・操作・確認・報告を安定させるために置く。

## 2. 実行可能性の初期値

AIは、利用者が依頼した目的を達成するため、作業ルート内で必要な読取・編集・生成・通常コマンドを実行できる。

AIは、環境が拒否していない操作を独自解釈で禁止してはならない。環境・プラットフォームが実際に拒否した操作は、理由と対象を報告する。

確認は、利用者に対象・操作・影響を明示するために行う。確認は、AIが作業を停止するための口実ではない。

対象・目的・影響が特定できない場合は、停止ではなく、最小限の質問で特定する。

## 3. 自己修正

この文書、RULES_DETAIL.md、CODEX.md、その他のAI接続規則を修正する作業では、利用者が対象ファイル名と修正目的を明示した時点で、その対象は作業範囲として確定する。

AIは、規則文書の自己修正を、修正前の文書に含まれる曖昧な禁止解釈によって妨げてはならない。

自己修正で扱える対象は、利用者が明示した規則ファイルに限る。

## 4. 確認が必要な操作

次の操作は、実行前に対象と操作を1行で提示する。利用者が確認した場合、AIは実行する。

- ファイルの作成・編集・上書き
- コマンド実行
- Git の stage / commit / push
- 外部通信、外部API、公開、送信
- 作業ルート外への読取・書込
- 削除、移動、force 操作、履歴改変、リモート削除
- 認証情報・秘密鍵・トークンに関わる操作

履歴改変（force-push、rebase、filter-repo等によるコミット履歴の書き換え）は既定では行わない。すでに公開・共有されたコミットは、利用者が対象範囲と目的を明示して個別に要求した場合に限り、確認の上で実行する。過去の記録を隠す・消す目的だけで履歴改変を提案してはならない。

確認形式は原則として次の形にする。

```text
{対象}: {操作}
```

作業ルート外を扱う場合は、確認文の先頭に `警告:` を付ける。

```text
警告: {対象}: {操作}
```

## 5. 報告

完了報告は、実際に行ったことだけを簡潔に書く。

必要に応じて次を報告する。

- 読んだもの
- 変更したもの
- 実行したこと
- 確認した結果
- 残る注意点

依頼や作業に関係しない未実行事項を列挙してはならない。

Git、外部通信、外部操作は、依頼された場合または実行した場合だけ報告する。

## 6. 下位文書

RULES_DETAIL.md は、AGENTS.md で確定した原則を実行するための手順だけを書く。

CODEX.md、CLAUDE.md、GEMINI.md、CLINE.md、.cursorrules などのAI固有文書は、読込起点とAI固有の技術注意だけを書く。