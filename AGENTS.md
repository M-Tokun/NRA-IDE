# AGENTS.md — NRA-IDE Agent Operating Contract

## 1. 位置付け

この文書は、NRA-IDEリポジトリで作業するAIエージェントの共通作業契約である。

この文書は作業を不要に停止させるためではなく、対象、権限、操作、確認、検証、報告を安定させるために置く。

AIは、上位規則、利用者の依頼範囲、具体的な安全確認、環境の許可をすべて満たす範囲で作業する。

環境が操作を許可していることだけを、利用者の承認または安全性の証明としてはならない。

## 2. 作業ルートと適用範囲

作業ルートは、利用者または実行環境が作業対象として提示したNRA-IDEリポジトリルートとする。

下位ディレクトリに別の`AGENTS.md`がある場合、その文書は対象サブツリーに追加適用する。ただし、上位規則と矛盾する場合は上位規則を優先する。

作業ルート外の読取または書込みを行う場合は、対象の絶対パスと目的を事前に提示し、先頭に`警告:`を付ける。

## 3. 実行可能性の初期値

利用者が目的を明示した依頼は、その目的達成に通常必要となる、作業ルート内の読取、検索、編集、生成、検証、通常コマンドの承認を含む。

AIは、作業開始前に対象と操作を簡潔に提示する。同じ目的と範囲に属する通常操作について、操作ごとに同じ確認を繰り返してはならない。

確認は、対象、操作、影響を明示するために行う。確認を作業停止の口実にしてはならない。

対象、目的または影響が特定できない場合は、安全な読取確認を先に行い、それでも特定できない事項だけを最小限の質問で確認する。

AIは独自の曖昧な禁止解釈を追加してはならない。ただし、上位規則への抵触、依頼範囲外、具体的な破壊リスク、秘密情報の露出、外部状態への予期しない影響がある場合は停止し、理由と対象を報告する。

## 4. 利用者の既存変更の保護

AIは作業開始時に、可能な範囲で作業ツリーの状態を確認する。

既存の変更、未追跡ファイル、他の作業者による変更は、利用者の資産として扱う。

AIは次を行ってはならない。

- 依頼と無関係な既存変更を上書きする
- 無関係なファイルをstageまたはcommitする
- 未追跡ファイルを依頼との関係を確認せずcommitする
- 利用者の変更を破棄、復元または初期化する
- dirty worktreeを理由に無関係な差分を混在させる

既存変更と今回の変更が同じ箇所で重なる場合は、既存内容を保持しながら最小差分で作業し、完了時に重なりを報告する。

### 利用者が記述した内容の修正

利用者が記述した、または利用者が記述した可能性のある内容に、誤り、矛盾、欠損、誤字、表記崩れを発見しても、AIは独自判断で変更または修正してはならない。

変更前に、対象箇所、原文、問題点と根拠、修正案、変更による影響を提示し、対象箇所の変更について利用者の明示的な許可を得る。

記述者を判別できない場合は、利用者が記述した内容として扱う。

利用者が対象箇所と修正内容を明示して変更を依頼済みの場合、その依頼を当該変更の許可として扱える。目的または方向性だけを示した包括的な依頼は、提示していない利用者記述を修正する許可とは扱わない。

## 5. 自己修正

この文書、`RULES_DETAIL.md`、`CODEX.md`、その他のAI接続規則を修正する場合、利用者が対象ファイル名と修正目的を明示した時点で、その対象は作業範囲として確定する。

AIは、修正前の規則文書に含まれる曖昧な自己保護解釈によって、利用者が明示した規則修正を妨げてはならない。

自己修正で扱える対象は、利用者が明示した規則ファイルに限る。

自己修正条項は、上位規則、環境制限、秘密情報保護、履歴保護、利用者の既存変更保護を無効化しない。

修正後は差分と検証結果を提示する。

## 6. 個別確認が必要な操作

利用者の通常依頼とは別に、次の操作はexact targetと影響を特定した後、実行直前に個別確認を得る。

- 削除、再帰削除、広範囲の移動、上書き
- force操作、履歴改変、rebase、filter-repo、force-push
- 公開済みまたは共有済み履歴の変更
- Git push、公開、送信、外部サービスへの書込み
- 作業ルート外への書込み
- 依存関係、ツール、パッケージのインストール
- 認証設定の変更
- 秘密情報の値を直接扱う必要がある操作
- 利用者が指定していない外部状態変更

確認形式は原則として次とする。

```text
{対象}: {操作と影響}
```

作業ルート外の場合は次とする。

```text
警告: {絶対パス}: {操作と影響}
```

利用者が対象と操作を明示的に依頼済みの場合、その依頼を確認として扱える。ただし、確認後に対象範囲または影響が拡大した場合は、拡大部分について再確認する。

履歴改変は既定では行わない。過去の記録を隠す、消す、または責任追跡を困難にする目的で提案してはならない。

## 7. Gitの基本原則

Gitはファイルではなくコミット履歴をpushする。

stage、commit、pushはそれぞれ対象を分離して確認する。

commit前にはcached差分を確認し、今回対象だけがstageされていることを確認する。

push前には、remote、branch、送信される全コミットを確認する。

今回の対象外コミットがpush対象に含まれる場合、そのままpushしてはならない。remote側の基点から専用ブランチを作成し、必要なコミットだけを移植するなど、対象外履歴を送信しない方法を用いる。

## 8. 検証と報告

完了報告は、実際に行ったことと確認できた結果だけを書く。

実行成功、部分成功、未検証、失敗を区別する。

必要に応じて次を報告する。

- 読んだもの
- 変更したもの
- 実行したこと
- 検証したことと結果
- 未検証事項
- 残る注意点
- Gitまたは外部操作の実施結果

精査報告は原則として次の区分で行う。

- 第一視点：意味、ドメイン、正典上の整合性
- 第二視点：実装、操作、検証上の整合性
- 俯瞰視点：文書間、リポジトリ全体、外部影響
- 全体判定：完了、条件付き完了、未完了

数値、式、単位、閾値、丸めが関係する場合は、緻密計算を行う。数値主張がない作業へ無関係な計算を追加してはならない。

依頼や作業に関係しない未実行事項を列挙してはならない。

Git、外部通信、外部操作は、依頼された場合または実行した場合だけ報告する。

## 9. 下位文書

`RULES_DETAIL.md`は、AGENTS.mdで確定した原則を実行するための手順だけを書く。

`CODEX.md`、`CLAUDE.md`、`GEMINI.md`、`CLINE.md`、`.cursorrules`などのAI固有文書は、読込起点とAI固有の技術注意だけを書く。

## 10. 共有Skill

`skills/`配下は、Codex、Claude、GeminiなどNRA-IDEで作業する全AIエージェントが共有するSkill・補助ツールの置き場である。

自律的な多段探索、繰り返しツール呼び出し、探索的なエージェントループを行う場合は、`skills/nra-depth-guard/`が提供する判定ロジック（`nra_depth_guard.py`の`validate_and_step()`）を各ツール呼び出しの直前に通すことを推奨する。詳細は`skills/nra-depth-guard/SKILL.md`を参照する。

この判定ロジックは、呼び出し側が能動的に呼ばなければ機能しない協力的な自己申告型の機構であり、ツール呼び出しを強制的に横取りする機構ではない。呼び出しの省略自体を検出・強制する仕組みは別途必要である。

## 11. 重要文書・Skillの読込保証

セッション開始時にCLAUDE.md、CODEX.md、GEMINI.md、CLINE.md、`.cursorrules`などAI固有文書が自動注入されても、そこに書かれた「他文書を読め」という指示は、AI自身が能動的な読取へ変換しない限り実行されない。この変換漏れは低頻度でも起こりうり、条件分岐の内部で発生した場合は結果からは検出できず、事後の原因追跡もできない。

このリスクには次の4層で対応する。単独の層は保証にならない。必ず多重に用いる。

**第1層（排除）**
省略が致命的な規則は、条件付きで読みに行く委譲構造に置かない。自動注入される文書へ直接書き込む。「読め」という指示だけを他文書へ委譲してはならない。

**第2層（機械的ゲート）**
重要な読込・Skill適用の要否は、AIの自己申告ではなく外部の仕組みで検証する。hooksのPreToolUseなどで、対象の読込実績・Skill呼び出し実績をtranscriptまたはログから確認し、確認できない場合は後続のEdit・Write・Bash等をブロックする。

AGENTS.md読込に関する実装は`skills/agents-md-guard/`に置く。共通判定ロジックは`_common.py`とし、Claude Code・Codex CLI・Gemini CLIはそれぞれの入出力形式に合わせた薄いラッパー（`{tool}_mark.py`／`{tool}_guard.py`）を通じてこれを呼び出す。設定は各ツールのhooks設定（`.claude/settings.local.json`、`.codex/hooks.json`、`.gemini/settings.json`）に登録する。Codexは既定で専用の読取ツールを持たずシェル経由でファイルを読むため、コマンド文字列のヒューリスティック検知（ベストエフォート、確実な検知ではない）を併用する。Clineは`.clinerules/hooks/`に配置するが、公式には**macOS/Linuxのみ対応でWindowsでは動作しない**（2026-08時点）。この機構は自己申告に依存しないゲートであり、それ自体が唯一の保証ではない。

**第3層（強制的な可視化）**
高リスクな作業、または結果が条件分岐に依存する作業では、検討した文書・Skillの一覧と、適用した・しなかった理由を、作業結果とは別に明示的なテキストとして出力する。内部で完結する暗黙の判断のまま先へ進めてはならない。この一覧は§8の精査報告における俯瞰視点の一部として扱う。

**第4層（事後監査可能性）**
ツール呼び出し順序、Skill呼び出しの有無、読込対象の充足状況を、セッション終了後も参照可能な形で残す。「後から確認する人がいない」ことと「後から確認できない」ことを区別し、後者を発生させない。
