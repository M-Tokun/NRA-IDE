# AGENTS.md — エージェント最小限カーネル (ser Japanese　only)

デフォルトモードは**読み取り専用 (READ-ONLY)** とする。

明示的に要求されたファイル、または `WORKFILES_INDEX.md` にリストされているファイルのみを編集すること。

対象、権限、またはアクションが不明確な場合は、停止して報告すること。決して推測で動いてはならない。

## 境界条件 (Boundary)

許可されるアクションは、明示的でなければならない。

アクションが明示的に許可されていない場合、それは**禁止 (forbidden)** される。

## 事前停止要件 (Stop Before)

以下の操作を行う前には、**必ず停止して報告**すること：

* リスト外のファイルの編集
* ファイルの移動、コピー、削除、または名前変更
* フォーマッタや自動修正 (auto-fix) の実行
* 依存関係またはロックファイルの変更
* 設定ファイルの変更
* ネットワークの使用
* 機密情報 (secrets) へのアクセス
* ステージング (`git add`)、コミット、またはプッシュ
* 一括操作 (bulk operations)

## 危険な単語 (Dangerous Words)

「整理 (organize)」「クリーンアップ (clean up)」「修正 (fix)」「改善 (improve)」「リファクタリング (refactor)」「フォーマット (format)」「整頓 (tidy)」という単語は、ファイルの移動、コピー、削除、名前変更、グローバルフォーマット、Git操作、または広範囲の編集を**許可しない**。

## Git規則 (Git)

* 編集エージェントはコミットしてはならない。
* コミットエージェントは編集してはならない。
* `git add -A`、`git add .`、`git add --all` は決して使用してはならない。
* コミットおよびプッシュには、それぞれ個別の明示的な承認が必要である。

## 保護対象 (Protected)

明示的に承認されない限り、`.git`、`.ssh`、`.env`、機密情報 (secrets)、トークン、秘密鍵、または外部のユーザーディレクトリには**決して触れてはならない**。

## 必須の読み込み (Required Reads)

* ファイル編集、Git操作、移動/コピー/削除/名前変更、インストール、ネットワークアクセス、依存関係の変更、設定の変更、または不確実性がある場合は、事前に `RULES_DETAIL.md` を読み込むこと。
* NRA-IDEの構造的推論を行う前に、`llms.md` を読み込むこと。

## フェイルクローズ (Fail-Closed)

不確実な場合は、停止して以下を報告すること：

* 不明確な点
* 提案するアクション
* 影響を受けるパス
* 必要な承認
