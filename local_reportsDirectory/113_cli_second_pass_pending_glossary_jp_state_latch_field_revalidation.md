# NRA-IDE 第2次CLI精査 継続Report — Glossary JP 状態・ラッチ項目再検証（修正待ち）

- 実施日: 2026-07-16 JST
- 対象: `docs/ja-JP/ai/12_glossary_JP.md`
- 現在SHA-256: `BD4D873627B6EF9CA971C40A78C3D1B1EC6DDA9371D85DFE293B0CFA8EA8E9BA`
- 既存完了Report: `73_cli_second_pass_glossary_jp_revalidation_complete.md`
- 横断再集計: `110_cli_second_pass_cross_file_rescan.md`
- 状態: 利用者判断待ち（対象本文は未編集）
- RAW監査報告01～17: 変更なし
- CHKポイント: 作成なし
- Git stage / commit / push: 実施なし

## 1. 問題箇所

133行目の最終固定証言定義は、固定項目と旧ラベルの位置付けを次のように記述している。

```text
少なくとも、正規状態、確定した構造値、境界条件、ラッチ状態、外部人間監査の経路を固定項目として記録します。旧ラベルは別の正規状態ではありません。
```

同ファイルの確定済み分類は`正典七状態`を用い、権限分離一覧では`状態`と`不可逆ラッチ`を別対象としている。この1文だけが`正規状態`と`ラッチ状態`を用いている。

## 2. 正典境界・影響

正典状態と不可逆ラッチは別対象である。不可逆ラッチは追加の状態名ではなく、同一履歴内で解除されない保持条件である。`ラッチ状態`のままでは、固定証言スキーマの実装時に正典状態と不可逆ラッチを同じ状態分類へ統合する余地が残る。`正規状態`は、この固定証言が記録する正典状態フィールドと、旧ラベルが正典状態ではないことを指す箇所では用語が不一致になる。

判定: `TERMINOLOGY_CONFLICT / STATE_MODEL_CONFLICT`

## 3. 推奨修正案

133行目の1文だけを次の範囲で限定修正する。

- 2箇所の`正規状態`を`正典状態`へ変更する。
- `ラッチ状態`を`不可逆ラッチの作動状態`へ変更する。

推奨文:

```text
少なくとも、正典状態、確定した構造値、境界条件、不可逆ラッチの作動状態、外部人間監査の経路を固定項目として記録します。旧ラベルは別の正典状態ではありません。
```

Report 73で整合済みの他の用語定義、完全な非再利用一覧、履歴境界、Markdownは変更しない。

利用者承認前に対象本文は編集しない。
