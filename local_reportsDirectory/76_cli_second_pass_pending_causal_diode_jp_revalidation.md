# NRA-IDE 第2次CLI精査 継続Report — Causal Diode JP 再検証（修正待ち）

- 実施日: 2026-07-16 JST
- 対象: `docs/ja-JP/ai/03_causal_diode_JP.md`
- 現在SHA-256: `83D55D76A5E8D13A0C3940D4E3D31EDF575EF8FEF9A48E9AF6D0111FD00EFC52`
- 位置付け: Report 48以後の横断残存検査で検出した表現を、Report 20の完了状態および英語版再検証結果と照合した再検証記録
- 状態: 利用者判断待ち（対象本文は未編集）
- RAW監査報告01～17: 変更なし
- CHKポイント: 作成なし
- Git stage / commit / push: 実施なし

## 1. 照合結果

Report 20で確定した次の主要境界は、現在の対象ファイルにも保存されている。

- 因果ダイオードは`Cause-Side → Effect-Side`だけの完全一方向経路である。
- 古い経路は旧Effect-Sideで終端する。
- 旧Effect-Sideから新旧いずれのCause-Sideにも矢印はない。
- 新履歴は独立した新対象、新しいCause-Side観測・履歴・事前固定規則、新しい因果ダイオードから開始する。
- 旧Effect-Side値のimport、名称変更、再構成、転用を禁止している。

現在SHA-256はReport 20の完了時SHA-256と一致し、同Report後の未記録変更はなかった。

## 2. 検出した残存問題

### 問題箇所A

現在の49行目は、Effect-Side記録がCause-Sideへ戻らないことを正しく示しているが、書換え権限を持たない正典対象を列挙していない。51行目の具体例も`δ`、`τ`、`irreversible_latched`、正規状態だけであり、`R`、正典閾値、規則、変換入力、更新根拠、出所が欠落している。

### 問題箇所B

現在の165行目は、LLM出力が`τ`を増やし、`δ`を減らし、`不可逆状態`を解除することを防ぐと記述している。

`不可逆状態`は、正典上別対象である`状態`と`不可逆ラッチ`を一語に結合している。また、因果ダイオードが保護する対象の要約が`δ`、`τ`、不可逆状態だけに限定されて見える。

## 3. 正典境界との衝突

- `状態`と`不可逆ラッチ`は別の正典対象として保持する必要がある。
- Effect-Sideの値、判断、成果物は、`R`、`δ`、`τ`、正典閾値、正典状態、不可逆ラッチ、規則、変換入力、更新根拠、出所を書き換える権限を持たない。
- これらを旧履歴内で更新することも、新履歴へimport、名称変更、再構成、再利用することもできない。

判定: `SEMANTIC_DRIFT / INCOMPLETE_PROHIBITION_SET`

## 4. 影響

現在の一方向性と履歴終端は維持されているが、禁止対象の列挙が不完全なため、Effect-Sideから規則、変換入力、更新根拠、出所などを変更できる余地、または状態と不可逆ラッチを同一対象として扱う余地が残る。

## 5. 推奨修正案

対象本文だけを限定修正する。

1. 49行目の段落へ、Effect-Side記録が`R`、`δ`、`τ`、正典閾値、正典状態、不可逆ラッチ、規則、変換入力、更新根拠、出所を書き換える権限を持たないことを追加する。
2. 165行目を、LLM出力が同じ正典対象のいずれも書き換えられない要約へ置き換える。
3. 既に整合済みの一方向ダイオード、旧経路終端、新履歴独立開始の記述は変更しない。

利用者承認前に`docs/ja-JP/ai/03_causal_diode_JP.md`は編集しない。
