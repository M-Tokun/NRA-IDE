# Temporal State Candidate

このディレクトリは、2026-08-12の区切り探索で作成した、学習型ゲートによる時間状態保持PoCを保存する。

この候補が扱うのは、前時刻の状態ベクトルと現在の推論出力を接続する時間方向の記憶である。NRA-IDEの外部安全カーネル、正規境界分類、実行権限制御、完全仲介または構造証言の実装ではない。

この候補は探索履歴として保持し、現行の`safety_kernel/`からは参照しない。

既知の課題は、ルートの`history.md`に記録する。

