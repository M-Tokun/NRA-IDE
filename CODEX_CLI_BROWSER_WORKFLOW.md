# Codex CLI・ブラウザ版 作業分担と検証・修正手順

## 1. 目的

この手順書は、NRA-IDEリポジトリでCodex CLIとブラウザ版を使い分け、次の事故を防ぐための作業標準である。

- 正典より新しい更新日時の旧仕様を誤って優先する。
- 複数のAIが同じファイルを同時編集する。
- 一部の文書だけを直し、コード・図・英日版へ矛盾を残す。
- 概念デモを検証済み実装または証明として扱う。
- 利用者の既存変更を上書きする。
- 検査未完了の状態で「全体整合済み」と報告する。

この文書は作業方法を定める。NRA-IDEの理論的正典を置き換えない。

## 2. 最上位の作業規則

作業開始時は、次の順で読む。

1. `AGENTS.md`
2. `RULES_DETAIL.md`
3. `CODEX.md`
4. 今回の依頼文
5. 対象範囲に関係する正典と監査報告

`AGENT.md`ではなく、実在するファイル名は`AGENTS.md`である。

ファイル作成・編集、コマンド、Git操作等は、実行前にAGENTS.mdの形式で対象と操作を提示する。

```text
{対象}: {操作}
```

作業ルート外の場合は、先頭に`警告:`を付ける。

秘密情報、password、env、秘密鍵、tokenはAIが操作せず、利用者が直接扱う。

## 3. CLIとブラウザ版の役割

### 3.1 Codex CLIを使う作業

CLIを実作業の主担当とする。

- リポジトリ全体のファイル検索、ハッシュ比較、参照関係確認
- コード・Markdown・JSON・設定ファイルの編集
- 複数ファイルの同期修正
- 構文検査、単体テスト、境界値テスト、リンク検査
- Git差分、未追跡ファイル、既存変更の確認
- 修正結果と検査ログの保存
- 変更対象を限定したstage、commit、push

CLIは「変更内容をファイルとテストで確定する場所」とする。

### 3.2 ブラウザ版を使う作業

ブラウザ版は判断・レビュー・視覚確認を主担当とする。

- 理論、数式、用語、文章の長文レビュー
- 修正候補の比較と設計判断
- PNG、JPG、SVG、HTML等の視覚確認
- CLIが提示した差分とテスト結果の第三者的再確認
- 利用者との優先順位、公開表現、採否の相談
- 外部一次資料が必要な事項の調査と出典確認

ブラウザ版は「何を正しいと採用するかを確認する場所」とする。

### 3.3 同時編集を避ける

CLIとブラウザ版で同じファイルを同時に編集しない。

1. 一方を編集担当に固定する。
2. 他方は読取・レビューだけ行う。
3. 担当交代前に、変更ファイル、未完事項、検査結果を引き継ぐ。
4. 引継ぎ後に`git status --short`と差分を再確認する。

## 4. 何を基準に正しさを決めるか

### 4.1 更新日時だけで採用しない

最終更新日は調査順を決める補助情報であり、正典順位ではない。

新しいファイルでも、旧状態語彙、旧式、会話ログ、未検証コードを含む場合がある。採否は次の順で決める。

1. 明示された正典順位
2. 正典の版番号と適用状態
3. 数式・状態遷移・定義域の内部整合
4. 参照実装と適合テスト
5. 更新日時

### 4.2 現在の基準資料

作業開始時点では、次を監査結果の入口とする。

- 全体報告: `local_reportsDirectory/17_global_consistency_including_docs.md`
- 詳細報告: `local_reportsDirectory/01_root_review.md`～`16_docs_overall_review.md`

報告は問題の所在を示す資料であり、正典そのものではない。報告と原文が食い違う場合は、原文、数式、コード、再現試験を再確認する。

### 4.3 v2.1で照合する主要核

- 正規比は`R = delta / tau`。
- `tau <= 0`を無限大のRへ変換しない。
- 記述領域外と、既知の危険進行を分離する。
- `R_warn`、`R_op`、`R_irrev`の役割を分離する。
- WARNING、CONFESSION、OUT_OF_DESCRIPTION_DOMAIN、IRREVERSIBLE_BOUNDARY、RUPTUREを混同しない。
- 不可逆境界到達後は、瞬間的なR低下だけでラッチを解除しない。
- 通常の自由生成を停止しても、独立したCause-Side構造証言は継続する。
- Effect-Sideの評価や出力からCause-Sideのdelta、tau、境界を更新しない。

この一覧は作業用チェック項目である。正確な規範文言は確定後の`AXIOMS.md`と`axioms.json`を参照する。

### 4.4 現行仕様・履歴・監査証拠を分離する

次の三つは関連するが、目的が異なるため同じファイルや同じ判断として扱わない。

1. **現行仕様を整合させる**：正規性、実装可能性、全体整合性を優先する。
2. **理論形成の履歴を保存する**：当時の原文、版、分岐、未解決事項を保存する。
3. **監査特性を記録する**：指摘、誤検出、過敏検出、判定条件を証拠として残す。

監査報告の詳しさや件数を理由に、報告内の提案を正典へ昇格させない。履歴文書に現行仕様との違いがあっても、当時の原文を無差別に書き換えない。

### 4.5 監査報告を固定する

現在の監査入口は`local_reportsDirectory`の17報告である。これらは原則としてRAW監査証拠として保持し、採否や分類のために本文を書き換えない。

分類結果は別manifestへ保存し、最低限次を記録する。

```text
report_id
source_files
finding_class
history_class
decision_status
decision_reason
applied_files
applied_commit
reviewed_at
```

報告のコピーを大量に作る代わりに、ファイル一覧、サイズ、SHA-256、作成日時、Git状態を持つ監査manifestで固定状態を識別する。

### 4.6 指摘を分類する

すべての指摘を同じ「不整合」として扱わない。次の分類を使用する。

| 分類 | 意味 | 基本処置 |
|---|---|---|
| `HARD_CONFLICT` | 同時に成立しない実質矛盾 | 優先修正候補 |
| `BOUNDARY_CONFLICT` | 境界値、比較演算子、発火・遷移条件の差 | 正典と再照合 |
| `SEMANTIC_DRIFT` | 条件、範囲、例外が変化している | 意味を確定して同期 |
| `AMBIGUOUS` | 複数解釈が可能 | 人間判断または未解決 |
| `SEMANTIC_EQUIVALENT` | 表現差はあるが意味構造が同じ | 原則修正不要 |
| `STYLE_VARIATION` | 語順、説明粒度、比喩、文体の差 | 修正対象外 |
| `FALSE_POSITIVE` | 監査の誤検出 | 証拠を添えて記録 |
| `OPEN_UNRESOLVED` | 情報不足で確定不能 | 穴埋めせず保留 |

意味同等性は、入力対象、発火条件、許可・禁止処理、発火後状態の四点で判定する。

### 4.7 履歴上の変化を分類する

旧記述は次のいずれかへ分類し、「古い」という理由だけで誤りにしない。

- `ERROR_AT_CREATION`: 当時の体系でも誤りだった。
- `SUPERSEDED`: 後続仕様に置き換えられた。
- `REFINED`: 後続仕様で精密化された。
- `SPLIT_CONCEPT`: 一概念が複数概念へ分離された。
- `MERGED_CONCEPT`: 複数概念が統合された。
- `TERMINOLOGY_SHIFT`: 意味を維持して名称・記号が変わった。
- `HISTORICAL_VALID`: 当時の版では整合していた。
- `UNRESOLVED_HISTORY`: 更新順・採用関係を確定できない。

理論史は監査報告だけから作らず、対象原本、版、参照関係、Directory階層を併用する。同時期の複数案や未解決分岐を、後付けで一本道の成功物語にしない。

### 4.8 Directoryを理論木として扱う

各Directoryについて、役割、上位・下位、正規性、現行・履歴・実験の別、関連Directoryをmanifestへ記録する。

検査は次の二方向に分ける。

- **枝内整合性**：同一Directory内の用語、数式、境界、状態、参照。
- **枝間整合性**：公理→定義→式→実装→sample→応用への意味継承。

sampleも継承検査の対象にする。ただし仮値、変数名、教材上の簡略化は、例示であることを明記した範囲で許容する。

## 5. 修正を始める前の手順

### Step 0: 監査終了状態を確認する

今回の指定範囲の監査は完了している。修正工程へ入る前に、次を確認する。

- `local_reportsDirectory`の報告数と一覧
- 空、作成途中、重複した報告の有無
- 報告manifestのハッシュとGit状態
- 監査対象・除外対象・基準正典

RAW監査報告を削除・統合・上書きせず、分類、採否、原本反映は別manifestで管理する。

### Step 1: 作業ツリーを確認する

CLIで次を確認する。

- 現在ブランチ
- `git status --short`
- 対象ファイルの既存差分
- 未追跡ファイル
- 削除状態のファイル

既存変更は利用者の変更として扱い、今回の修正と混ぜない。由来が不明な変更を復元、削除、移動しない。

### Step 2: 一回の修正単位を小さくする

一回の修正単位を「一つの規則または一つの同期関係」に限定する。

良い例:

- 正典参照順位だけを同期する。
- `G(r)`の大残差近似と飽和語彙だけを訂正する。
- tau非正値の状態遷移だけを参照実装へ追加する。

避ける例:

- theory、コード、docs、noteを一度に全面書換えする。
- 理論変更と文章整形を同じ差分へ混ぜる。
- 旧資料を分類せず一括置換する。

### Step 3: 変更前の受入条件を決める

編集前に次を文章で固定する。

- 何が現在不整合か。
- どの正典記述を基準にするか。
- どのファイルを変更するか。
- 何を変更しないか。
- どのテストが通れば完了か。

## 6. 間違いを減らす修正順序

下位資料から先に直さない。次の順序を守る。

### Phase 1: 最上位正典の内部整合

対象:

- `theory/AXIOMS.md`
- `theory/axioms.json`

確認事項:

- 正典参照順位
- tau発展式の条件
- 二重ゆらぎの必須・条件付き出力
- 状態、境界、ラッチ、証言の完全な対応
- MarkdownとJSONの意味一致

完了条件:

- JSON構文が正常。
- MarkdownとJSONの項目対応表に欠落がない。
- 同じ概念へ異なる規範を与えていない。

### Phase 2: 数式・記号辞書

対象:

- `FORMULA.md`
- `theory/THEORY.md`
- 必要な上位解説文書

確認事項:

- 正規Rと側別比、補助量、残差関数を別記号にする。
- `G(r)=r|r|/(k+|r|)`は大残差で`G(r) ~ r`とする。
- `G(r)`を有界飽和または厳密な沈黙域と呼ばない。
- 式ごとに定義域、単位、符号条件、有限性を明示する。

完了条件:

- 同一記号の別定義がない。
- 小残差・大残差・正負の境界値を数値確認できる。

### Phase 3: 単一参照実装

対象候補:

- `NRA-IDE_Architecture_public.py`の正規ソース
- 状態機械と機械可読仕様

実装すべき最低条件:

- tau非正値、NaN、非有限値を領域外へ分離する。
- infinityへの暗黙変換を行わない。
- 三境界と現行状態語彙を実装する。
- 不可逆ラッチを実装する。
- 独立構造証言を継続する。
- Cause-SideとEffect-Sideの更新権限を分離する。

必須テスト:

- 各境界の直前、同値、直後
- tauが正、ゼロ、負、NaN、正負のinfinity
- deltaが負、ゼロ、正、NaN、infinity
- ラッチ後にRが低下する場合
- 通常生成停止後の構造証言
- 上下因果チャネルの独立性

完了条件:

- 参照実装と正典状態表が一対一で対応する。
- テストが状態名だけでなく出力項目とラッチを検証する。

### Phase 4: theory下位文書

対象:

- Thesis
- SANDWICH_ARCH
- GOVERNANCE
- ETHICS
- theory内画像

作業:

- 上位正典への参照順位を統一する。
- 旧状態・旧閾値を更新する。
- 断定強度を証拠に合わせる。
- 旧画像は再生成またはlegacy化する。

### Phase 5: docs公開入口

対象:

- `docs/README.md`
- `docs/README_JP.md`
- docsの公開Python
- TOP図、基礎式図、状態図

作業:

- 利用者が最初に読む内容を参照実装と同期する。
- 英日を同じ変更単位で更新する。
- 旧・中間・現行資料をmanifestで分類する。
- 概念デモには`not validated`等の表示を付ける。

完了条件:

- READMEから辿るファイルが現行版だけになる。
- ローカルリンク切れがない。
- オフラインで必要な表示が成立する。

### Phase 6: nra-core

作業:

- normative implementation、experimental、legacy、invalidを分ける。
- papers、implementation、quantum、visualizationを参照実装へ同期する。
- 実行不能コードと概念デモをcoreの現行面から隔離する。

### Phase 7: note

noteは研究履歴として扱い、原文を無差別に現行化しない。

各ファイルへ次のいずれかを付ける。

- current explanatory
- legacy
- research hypothesis
- conceptual visualization
- invalid experiment
- derivative PDF
- external reference

履歴保存文書は、本文を改変するより、manifestと冒頭表示で位置付けを明確にする。

### Phase 8: 全体再検査

現在のリポジトリは`rg --files`基準で529ファイルである。ファイル数は作業開始時に再計測し、全範囲で次を検索する。

- 旧状態名
- 固定0.4、0.7、0.75、0.8等
- 全面沈黙、空文字、出力ゼロ
- tau非正値のinfinity化や小正数置換
- `R = max(...)`
- `R`を残差関数や別の比へ使用する箇所
- saturation、飽和応答
- reset、rollback、境界後のtau再構成
- proof、guarantee、唯一、必然、100%等の過大表現

残存箇所は、現行仕様、説明用例、legacy引用のどれかを分類する。文字列が残るだけで機械的に不合格とはせず、文脈を確認する。

### Phase 9: Issue Clusterで再評価する

多数の指摘を一件ずつ独立修正せず、共通原因へまとめる。

```markdown
# Issue Cluster: IC-000

- 主題:
- finding_class:
- history_class:
- 関連正典:
- 関係報告:
- 関係ファイル:
- 代表事例:
- 例外・反例:
- 正規表現または正規状態:
- 一括修正の可否:
- 履歴保存の必要性:
- 人間判断:
- 検証方法:
```

各Clusterでは、典型例だけでなく境界例、誤検出例、履歴的に妥当な例を残す。一つのClusterを修正、検証、Directory再照合してから次へ進む。

## 7. 各修正後の検証方法

### 7.1 最小検証

すべての変更で実行する。

- 対象ファイルの再読込
- `git diff --check`
- 対象限定の`git diff`
- 構文検査
- 参照リンク確認
- 同期対象との再照合

### 7.2 コード変更

- 既存テスト
- 変更した状態の境界値テスト
- 例外・不正入力テスト
- 最小実行例
- 変更していない利用者差分の確認

### 7.3 Markdown・JSON変更

- JSON parse
- Markdown見出し、表、コードフェンス
- 英日対応
- 正典参照順位
- 同一記号の定義検索
- ローカルリンクと画像参照

### 7.4 HTML・画像変更

- ブラウザ表示
- ボタン、slider、Auto Run、reset
- 表示値と内部計算値の一致
- 固定時刻や描画分岐による結論の埋込み確認
- 外部CDNなしの場合の表示
- 画像内の文字、式、状態名、版表示

## 8. CLIからブラウザ版への引継ぎ

次の形式で渡す。

```text
対象:
目的:
基準正典:
変更ファイル:
変更していない関連ファイル:
主な差分:
実行した検証:
検証結果:
残る判断事項:
現在のgit status:
```

ブラウザ版は、原文全体より先に次を確認する。

1. 受入条件
2. 差分
3. テスト結果
4. 正典との対応表
5. 視覚資料

## 9. ブラウザ版からCLIへの引継ぎ

判断を実装可能な形へ変換して渡す。

```text
採用する案:
採用理由:
変更対象:
変更禁止範囲:
正確な用語・式・状態:
必要なテスト:
完了条件:
```

「整合するように直す」だけでは渡さず、どの記述を基準に何を一致させるかを明記する。

## 10. Git手順

Git操作は次の順で行う。

1. 変更差分を提示する。
2. stageするファイルを確認する。
3. commit messageを確認する。
4. 現在branchとpush先remote/branchを確認する。
5. pushは個別にy/n確認する。

一回のcommitには一つの論理的変更を入れる。監査報告、正典変更、実装変更、生成画像を理由なく同じcommitへ混ぜない。

## 11. 停止して利用者判断を求める条件

次の場合は推測で選ばない。

- 最上位正典同士が競合し、どちらを採るかで状態機械が変わる。
- 数式の修正により既存データやAPI互換性が変わる。
- legacy資料を削除するか履歴保存するかの判断が必要。
- 医療、法務、安全規格等の外部検証なしでは表現を確定できない。
- 利用者の既存変更と同じ行を変更する必要がある。
- 新しい依存関係、外部通信、公開、pushが必要。

停止時は、競合箇所、選択肢、影響、推奨案を簡潔に示す。

## 12. 完了条件

「修正完了」は次をすべて満たす場合だけ使用する。

- 予定した対象ファイルを変更した。
- 受入条件を満たした。
- 必要なテストを実行し、結果を記録した。
- 上位・下位・英日・コード・図の必要な同期を確認した。
- 未関連の利用者変更を保持した。
- RAW監査報告を改変せず、採否を別manifestへ記録した。
- 指摘と採用決定、現行修正と履歴保存を分離した。
- 残る不整合を「なし」または具体的な未完事項として報告した。

一つのディレクトリが合格しても、全体再検査前に「リポジトリ全体整合済み」とは報告しない。

## 13. CLI開始用テンプレート

```text
最初にAGENTS.md、RULES_DETAIL.md、CODEX.mdを全文読む。
local_reportsDirectory/17_global_consistency_including_docs.mdを全体監査の入口とし、
対象に対応する詳細報告を読む。

作業開始前にgit status、現在branch、対象ファイルの既存差分を確認する。
更新日時だけで正典を決めず、正典順位、版、内部整合、参照実装、テストの順で判断する。

今回の対象: [ここに記入]
今回の目的: [ここに記入]
変更禁止範囲: [ここに記入]
完了条件: [ここに記入]

編集前に対象と操作を1行で提示し、変更は最小単位で行う。
修正後は対象限定テスト、差分、全体への残存影響を報告する。
stage、commit、pushはRULES_DETAIL.mdの確認順に従う。
```

## 14. ブラウザ版レビュー用テンプレート

```text
これは実装前または実装後のレビュー依頼です。

基準正典: [ファイルと版]
対象差分: [ファイル]
解決したい不整合: [内容]
CLIで実行済みの検証: [結果]

次を確認してください。
1. 正典との意味一致
2. 数式・状態遷移・定義域
3. 英日差
4. 証拠を超える断定
5. 図の表示と内部計算の一致
6. 追加すべき境界値・反例テスト

ファイルは編集せず、問題点と推奨修正だけを返してください。
```

## 15. 最短運用チェックリスト

- [ ] AGENTS.md、RULES_DETAIL.md、CODEX.mdを読んだ。
- [ ] git statusと既存差分を確認した。
- [ ] 正典と更新日時を混同していない。
- [ ] 現行仕様、履歴、監査証拠を分離した。
- [ ] RAW監査報告を固定し、分類は別manifestへ記録した。
- [ ] 指摘を原因別・履歴別に分類した。
- [ ] 一回の修正単位を限定した。
- [ ] 受入条件を編集前に決めた。
- [ ] 正典から下位資料の順で修正した。
- [ ] 境界値、不正値、不可逆ラッチ、証言継続を検査した。
- [ ] 英日、コード、JSON、図の同期を確認した。
- [ ] 概念デモを証明または実験と呼んでいない。
- [ ] 利用者の既存変更を保持した。
- [ ] Git操作を確認順どおりに進めた。
- [ ] 全体再検査前に全体合格と報告していない。
