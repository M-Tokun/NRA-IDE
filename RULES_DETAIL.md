# RULES_DETAIL.md — NRA-IDE 実行詳細規則

## 1. 作業開始

1. `AGENTS.md`を読む。
2. 作業ルートを確認する。
3. 作業ツリーの既存変更と未追跡ファイルを確認する。
4. 今回の対象、既存差分、対象外差分を分離する。
5. 対象と操作を利用者へ簡潔に提示する。

利用者が目的を明示している通常操作は、同じ範囲内で確認を繰り返さない。

## 2. ファイル変更

ファイル変更前に、対象ファイルと変更目的を確認する。

既存差分がある場合は、それを保持して最小差分で編集する。

利用者が記述した、または記述者を判別できない箇所に修正が必要と判断した場合は、次の順で進める。

1. 対象ファイルと対象箇所を特定する。
2. 原文を変更せず保持する。
3. 問題点と判断根拠を示す。
4. 修正案と影響範囲を示す。
5. 対象箇所を変更してよいか利用者へ確認する。
6. 明示的な許可を得た対象だけを変更する。

誤字、表記崩れ、明白に見える誤りであっても、この確認を省略しない。

利用者が対象箇所と修正内容を明示して変更を依頼済みの場合は、その依頼を当該変更の許可として記録し、同じ確認を繰り返さない。

変更後は次を確認する。

- 想定したファイルだけが変更されている
- 利用者の既存差分が失われていない
- 意図しない生成物が追加されていない
- `git diff --check`で表記上の問題がない
- 関連するテストまたは検証が成功する
- 数式latexがgit準拠になっている。

## 3. Git操作

Git操作は次の順で進める。

1. `git status`で既存差分を確認する。
2. 今回の対象ファイルを確定する。
3. 関連テストと検証を実行する。
4. 今回対象だけを明示的なパスでstageする。
5. cached差分と対象ファイル一覧を確認する。
6. 内容に対応したcommitメッセージを決める。
7. commitする。
8. 現在branch、upstream、remoteを確認する。
9. remoteへ送信される全コミットを列挙する。
10. exact remote/branchと送信コミットを提示してpush確認を得る。
11. 通常の非force pushを実行する。
12. remote追跡状態を確認する。

push前には、原則として次を確認する。

```text
git log --oneline <remote>/<branch>..<branch>
```

pushはファイル単位ではなくコミット単位で行われる。ファイル数によってpushを分割してはならない。

今回対象外のコミットが含まれる場合は、現在branchをそのままpushしない。

その場合は、remote側の基点から専用branchを作成し、今回必要なコミットだけをcherry-pickして検証後にpushする。

force-push、rebase、filter-repo、履歴削除は、利用者が対象範囲と目的を個別に明示した場合に限る。

## 4. 秘密情報

password、token、秘密鍵、`.env`の値などを、出力、ログ、commit、メッセージへ含めてはならない。

秘密値を確認するためだけにファイルを開いてはならない。

認証済みCLIが既存認証を内部的に使用することと、AIが秘密値を直接読み取ることは分離する。

秘密値の入力、変更、再発行が必要な場合は、利用者へ直接操作を依頼する。

漏えいの可能性を検出した場合は、値を再掲せず、対象ファイルと必要な対処だけを報告する。

## 5. 依存関係の導入

依存関係を導入する前に、次を提示して確認を得る。

- package名
- version
- 導入目的
- 書込み先
- project依存かglobal環境か
- lockfileへの影響
- 既知の注意点

安全性確認は、対象エコシステムに適した一次情報を優先する。

- 公式package registry
- 公式upstream repository
- 公式release notes
- lockfile、checksum、署名
- GitHub Advisory Database
- OSV.dev
- package manager標準のaudit機能

Qiita、Snyk、ブログ、掲示板は補助情報として扱い、問題の指摘がないことを安全性の証明にしてはならない。

npm公式はnpm系packageにだけ用いる。Python、Rust、Java等には各エコシステムの公式情報を用いる。

ネットワーク制限等で確認できない場合は、未確認事項として報告し、推測で安全と断定しない。

## 6. 作業ルート外操作

作業ルート外を扱う前に、解決済みの絶対パス、操作、目的、影響を提示する。

```text
警告: {絶対パス}: {操作と影響}
```

削除または再帰移動では、対象が意図したディレクトリ内にあることを読取確認してから実行する。

広いパス、未解決変数、曖昧なglobを破壊操作の対象にしてはならない。

## 7. 失敗時の処理

コマンドまたは操作が失敗した場合は、成功したと報告してはならない。

安全な読取調査で原因を特定する。

同じ失敗を無条件に反復しない。

権限、環境、外部状態、利用者判断が必要な場合は、失敗理由、対象、試したこと、次に必要な操作を簡潔に報告する。
