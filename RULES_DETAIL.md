# RULES_DETAIL.md — エージェント詳細規則

## 1. 権限モデル (Permission Model)

すべての対象を以下のように分類すること：

* **confirmed (確定)**：ユーザーまたは `WORKFILES_INDEX.md` によって明示的に指定されている
* **unconfirmed (未確定)**：関連する可能性があるが、承認されていない
* **forbidden (禁止)**：機密情報、`.git`、`.ssh`、`.env`、認証情報、外部ディレクトリ

> [!NOTE]
> 「確定」は書き込み可能を意味しない。
> 「未確定」は行動前に停止し、報告することを意味する。
> 「禁止」はフェイルクローズ（安全側に倒して遮断）を意味する。

## 2. アクションクラス (Action Classes)

**読み取り (Read):**

* 承認されたスコープ内でのみ許可

**編集 (Edit):**

* 明示的に承認されたファイルのみ許可

**波及操作 (Propagation):**

* コピー、エクスポート、大量のファイル生成など
* 明示的な承認が必要

**構造変更 (Structural):**

* 移動、名前変更、削除、分割、結合、再構成
* 正確かつ明示的な承認が必要

**状態確定 (State-finalizing):**

* `git add`、`commit`、`tag`、`push`、リリースなど
* 個別の明示的な承認が必要

## 3. 危険な操作 (Dangerous Operations)

正確な承認なしに以下の操作を実行してはならない：

* 移動 (move)
* コピー (copy)
* 削除 (delete)
* 名前変更 (rename)
* 再帰的カウント・操作 (recursive operation)
* ワイルドカード操作 (wildcard operation)
* 一括操作 (bulk operation)
* グローバルフォーマッタの適用 (global formatter)
* 自動修正 (auto-fix)
* 依存関係のインストール/更新 (dependency install/update)
* ロックファイルの更新 (lockfile update)
* Gitのステージング (Git staging)
* Gitのコミット (Git commit)
* Gitのプッシュ (Git push)
* ネットワークアクセス (network access)
* 機密情報へのアクセス (secret access)

## 4. 整理規則 (Organize Rule)

「整理（Organize）」とは**提案のみ**を意味する。

**許可される事項:**

* ファイル一覧の取得
* 重複候補の検出
* 移動/名前変更/削除計画の提案
* リスクの報告

**禁止される事項:**

* 実際の移動
* 実際のコピー
* 実際の削除
* 実際の名前変更
* 実際のコンテンツ編集
* Git操作

## 5. フォーマット規則 (Format Rule)

フォーマットは書き込み操作である。

明示的な承認なしに、グローバルフォーマッタ、自動修正、インポートのソート、または改行コードの変換を実行してはならない。

## 6. Git規則 (Git Rule)

Git操作の前に、以下のみを検査すること：

* `git status --porcelain`
* `git diff --name-only`

以下は決して使用してはならない：

* `git add -A`
* `git add .`
* `git add --all`

承認された正確なファイルのみをステージングすること。

**コミットに必要な条件:**

* 承認されたファイルリスト
* レビュー済みの差分（diff）
* 進行中の編集エージェントが存在しないこと
* 予期しないファイルが存在しないこと
* 機密情報が含まれていないこと
* 関連のないフォーマット変更が含まれていないこと

> [!IMPORTANT]
> プッシュ（Push）には別途個別の承認が必要。

## 7. 並行エージェント規則 (Parallel Agent Rule)

**並行安全（競合しない）:**

* 独立したワークツリー
* 独立したブランチ
* 独立した出力フォルダ
* 独立したファイル

**並行危険（競合する）:**

* 同一の `README`
* 同一の `AGENTS.md`
* 同一の `RULES_DETAIL.md`
* 同一の `llms.md`
* 同一の インデックス/設定/パッケージ/ロックファイル
* 同一の Gitステージングエリア

重複が発生する場合は、直列化（順次実行）すること。

## 8. 出力規則 (Output Rule)

生成されたレポートは `local_reports/` に格納する。

明示的な承認がない限り、既存のファイルを上書きしてはならない。

## 9. 機密情報規則 (Secret Rule)

機密情報を読み取り、出力、コピー、要約、コミット、または送信してはならない。

機密情報が出現した場合は、その内容を明かすことなく操作を停止し、報告すること。

## 10. 最終報告 (Final Report)

作業完了後、以下を報告すること：

* 読み取ったファイル
* 変更したファイル
* 実行したコマンド
* 実施したチェック
* 実行しなかった事項
* リスクまたは注意事項

## 11. フェイルクローズ (Fail-Closed)

対象、権限、アクション、スコープ、Gitの状態、機密情報のリスク、一括操作の影響、依存関係への影響、設定への影響、または生成ファイルの影響が不明確な場合は、直ちに停止（Fail-Closed）すること。