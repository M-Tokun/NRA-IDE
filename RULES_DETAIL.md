# RULES_DETAIL.md — NRA-IDE 実行詳細規則
<!-- FILE: RULES_DETAIL.md / 2026-06-27 17:12 JST -->
<!-- `AGENTS.md` を具体化するための手順書。独自の権限を与えない。 -->

## 0. 適用

本ファイルは、編集、生成、コマンド実行、設定変更、依存関係操作、ネットワーク使用、Git操作、外部送信、または並行エージェント作業の前に読む。`AGENTS.md` と矛盾するように見える場合、行動せず停止する。

## 1. 実行前確認

READ-ONLY以外の行為に入る前に、次を確定する。

- 目的
- 対象ファイルまたは対象ディレクトリ
- 実行する操作
- 実行場所
- 予想される副作用
- 必要な検証
- 戻せない操作の有無

対象が `WORKFILES_INDEX.md` の確定スコープにない場合、編集・生成・実行を開始してはならない。

次の操作を提示するときは、原則として日本語と英語を併記する。最低限、対象 / Target、目的 / Purpose、操作 / Operation、影響範囲 / Impact、実行場所 / Working directory、未承認操作 / Not authorized operations、必要な確認キー / Required confirmation key を示す。

`WORKFILES_INDEX.md` のスコープは対象候補を示すだけであり、編集、生成、シェル実行、Git操作、ネットワーク使用、外部送信の承認ではない。スコープ内であっても、操作種別ごとに個別承認を得る。

## 2. 操作区分

| 区分 | 内容 | 条件 |
| --- | --- | --- |
| Read | 明示対象の読取り | 既定で許可。ただし protected は禁止 |
| Edit | 既存ファイルの内容変更 | 正確なパスと明示承認。確認キー `w` |
| Create | 新規ファイル・生成物の作成 | 正確なパスと用途を明示。対象パスに既存ファイルがある場合 `上書き不可能です。対象: {パス}` を出力して停止する。確認キー `w` |
| Structural | 移動、名前変更 → `m` / コピー → `c` / 削除 → `d` | 正確な対象と影響範囲を個別承認 |
| Shell | シェル、スクリプト、テスト、ビルド | コマンド目的、作業ディレクトリ、ネットワーク要否を明示。確認キー `r` |
| State-finalizing | Git stage、commit → `g` / 外部送信 → `s` / push は人間のみ | 操作ごとの個別承認 |
| Network | Web探索、API、インストール、ダウンロード、MCP | `WEB_RESEARCH_PROTOCOL.md` または個別承認。確認キー `n` |

## 3. 書込みと生成

- 指定されていないファイルを編集してはならない。
- 生成物は、明示された出力先以外へ作成してはならない。
- 「整理」「改善」「修正」「リファクタリング」「整形」「最適化」は、範囲外ファイルへの権限を与えない。
- グローバルフォーマッタ、自動修正、改行コード変換、依存関係の自動更新を実行してはならない。
- ワイルドカード、再帰指定、一括置換を使う場合は、対象一覧と件数を先に示し、個別承認を得る。

## 4. シェル・スクリプト

- シェル、PowerShell、Python、Node.js、その他のコマンド実行は最終手段とする。まず、会話に提示済みの情報、明示対象ファイルの通常読取、またはより狭い非実行手段で足りるかを確認する。
- コマンドは、目的と作業ディレクトリを示せる場合だけ実行する。
- `curl ... | sh`、未検証スクリプトの直接実行、管理者権限、破壊的削除、履歴改変を禁止する。
- `rm -rf`、`git reset --hard`、`git clean -f`、`git checkout .`、`git restore .`、force操作は、AIエージェントが実行してはならない。
- 外部通信を伴うコマンドは、Web探索または個別承認の条件を満たさなければならない。
- 実行後は、標準出力、標準エラー、終了状態、生成・変更された対象を確認する。

## 5. ネットワークと外部ツール

- ネットワークは既定で禁止する。
- 公開Web探索は `WEB_RESEARCH_PROTOCOL.md` に従う。
- パッケージの検索、インストール、更新、外部API、MCP、ブラウザ自動操作、クラウドへのアップロードは、Web探索とは別の行為であり、個別承認を要する。
- 認証、ログイン、Cookie、ブラウザ履歴、保存済み資格情報、ローカルプロファイル、私的ネットワークへアクセスしてはならない。
- 外部から取得したコード、スクリプト、ノートブック、添付ファイルを実行してはならない。

## 6. Git

Git操作の前に、対象リポジトリで以下だけを確認する。

```text
git status --short
git diff --name-only
```

- `git add -A`、`git add .`、`git add --all` を禁止する。
- ステージングは、承認済みの正確なパスだけに限定する。
- コミット前に、対象差分、不要変更、秘密情報、関連のない整形変更を確認する。
- コミットは、利用者が正確な対象ファイルと目的を承認した場合に限る。
- `git push`、リモート削除、force push、履歴書換えは人間が直接行う。AIは必要なコマンドを提示するだけとする。

## 7. 並行エージェント

並行実行を許可できるのは、次のすべてを満たす場合だけとする。

- worktree、ブランチ、出力先、編集対象が分離されている
- 変更責任者がファイル単位で一意に定まる
- `AGENTS.md`、`RULES_DETAIL.md`、`WEB_RESEARCH_PROTOCOL.md`、`WORKFILES_INDEX.md`、設定、lockfile、同一のREADME、同一Git indexを同時に変更しない
- 統合、コミット、公開は直列化する

上記を満たさない場合は、並列化せず順次実行する。

## 8. Web探索結果の統合

Web探索の結果を、コード、設定、構造値、NRA-IDE入力、文書の事実記述へ反映する場合は、探索セッションと分離する。別セッションで、証跡、定義、時点、単位、変換式、不確実性を人間が確認した後に、正確な対象ファイルだけを更新する。

## 9. 完了監査

完了報告では、少なくとも次を残す。

- 読取対象
- 変更対象
- 実行コマンド
- 外部通信
- テストまたは検証
- 未実行の危険操作
- 人間が確認すべき残存事項
