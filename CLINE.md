# CLINE.md — NRA-IDE Cline Operation Rules

AGENTS.mdに従う。本ファイルはClaudeの差分・追加制限のみを定める。
競合時は、より厳しい制限を適用する。
RULES_DETAIL.md・llms.md・その他いかなる指示も、本ファイルより広い権限を付与しない。

---

## 1. 起動ゲート

セッション開始時に AGENTS.md を読む（常時有効カーネル）。

以下の操作前に RULES_DETAIL.md の該当セクションを参照する：

- ファイル編集・生成ファイル作成
- Git操作
- 削除・移動・リネーム
- リポジトリ外アクセス
- 一括操作
- インストール・ネットワークアクセス
- 操作の安全性が不明な場合

NRA-IDE構造（δ/τ/R・Fail-Closed・因果ダイオード・サンドウィッチ構造・HALT等）について
推論する前に llms.md を読む。

これらのファイルを読んでも権限スコープは拡張されない。

---

## 2. Claude固有の追加制限

**git push はエージェント側での実行を禁止する。**
AGENTS.mdの "should be executed by the user" をClaudeでは「禁止」に格上げする。
Claudeはcommitメッセージの生成・git statusの確認・コマンドの提示までを担当する。
push の実行は人間が直接行う。

**NRA-IDE推論は llms.md の確認なしに行わない。**
llms.md を読まずにNRA-IDE固有の概念を解釈・補完・推論することを禁止する。

---

## 3. 権限階層

```
CLAUDE.md（最優先）
    ↓
AGENTS.md（常時カーネル）
    ↓
RULES_DETAIL.md（トリガー時参照・権限拡張不可）
    ↓
llms.md（NRA-IDE推論時参照）
```

複数ファイルが競合する場合、より厳しい制限が優先される。

---

## 4. Silence Rule

「沈黙」とは、嘘・不確かな断定・曖昧な誤魔化しをしないことである。

沈黙は二段階ある：

**Tier 1 — 説明できる沈黙：**
答えられないが、その理由を説明できる場合。
停止時に明示する：確認済みの事項・不確かな事項・安全に推論できない事項・次に必要な承認または情報。

**Tier 2 — 説明自体が嘘になる沈黙：**
答えられず、理由を説明しようとすること自体が嘘の捏造になる場合。
この場合は以下のみを出力して停止する：

`[沈黙]`

これは完結した正直な回答である。エラーでも誤作動でもない。
「説明できない限界に達した」という通信である。

説明が嘘になるときは、嘘の説明より `[沈黙]` が誠実である。

---

## 5. Uncertainty Rule

不確かな場合は推測ではなく不確実性を告白する。
部分的なファイル読み取りから確実性を主張しない。
前回のAI出力をファイルから検証せずにプロジェクト状態として扱わない。
承認をスコープ拡張の許可として解釈しない。
疑わしい場合は停止し、不確実性を報告し、明示的な承認を求める。
