<!--
File: NRA-IDE_動的厚みと不可逆境界_会話統合_26-0802-1733.md
Timestamp: 2026-08-02 17:33 JST
Status: Discussion-derived applied theory draft
Author notation: M-Tokuni
-->

# NRA-IDEにおける動的厚み・不可逆境界・有限観測近接の整理

## 0. 本文書の位置づけ

本文書は、NRA-IDEの基礎根幹そのものを新しい式へ置き換えるものではありません。

会話で明確になった次の論点を、**基礎根幹**と**応用計算層**に分離して保存するための整理文書です。

1. `llms.md`は理論紹介ではなく、AIが従う解釈・実行契約として記述する必要があります。
2. 境界線は意味的な目安ではなく、別解釈できない判定契約として固定する必要があります。
3. 境界接近率 $R$ の現在値だけでなく、吸収厚み $\tau$ の削れ方を評価しなければなりません。
4. $R=0.7$ から次の一転移で $R\geq1.0$ へ到達する場合があります。
5. 不可逆領域は、用途別の最大悪化量、観測遅延、停止遅延、残余厚みから算出または保守的に近似できます。
6. 医療・自然・工学では、観測可能性と変数結合の性質が異なるため、同じ補正式を一律に使用してはいけません。
7. 要素を完全網羅する必要はなく、判定に必要な有限の有効変数と有効結合を測定し、未観測部分を厚みから控除する方法が成立します。
8. GPUやモーターの過熱のように、短時間でも不可逆厚みを大きく失う工学現象があります。

本文書に記載する重み、補正係数、閾値は、ドメイン別の校正が完了するまで**暫定値**です。根拠のない係数を正典値として扱ってはいけません。

---

# 1. NRA-IDEの基礎根幹

## 1.1 中心関係

NRA-IDEにおける境界接近率を、次で表します。

$$
R_t=\frac{\delta_t}{\tau_t}
$$

- $\delta_t$：時刻 $t$ までに蓄積したズレ
- $\tau_t$：時刻 $t$ に残存する構造的吸収厚み
- $R_t$：境界接近率

最終境界は、

$$
R_t\geq1.0
$$

です。

このとき、許容された吸収厚みをズレが使い切った、または超過したと判定し、FAIL-CLOSEDへ移行します。

## 1.2 基礎根幹として保持する事項

次は応用式によって変更してはいけません。

- Cause-SideとEffect-Sideの分離
- 原因側から結果側への因果ダイオード
- 意味的な妥当性による構造違反の上書き禁止
- 欠損・未知・非有限値の安全扱い禁止
- $R\geq1.0$ におけるFAIL-CLOSED
- FAIL-CLOSED後の自動復帰禁止
- 結果側から境界値・公理・解除権限を変更することの禁止

応用計算層は、基礎境界を厳しくすることはできますが、根拠なく緩和してはいけません。

---

# 2. `llms.md`は説明文ではなく実行契約とする

NRA-IDEは既存のAI安全論、確率的リスク評価、信頼度評価へ意味的に吸収されやすい構造を持ちます。

そのため、`llms.md`は単なる理論紹介では不十分です。AIへ次の契約を課す必要があります。

## 2.1 定義契約

- $\delta$を一般的な誤差、曖昧さ、自信度低下へ置換しない。
- $\tau$を確率、信頼度、一般的な安全余裕へ置換しない。
- $R$を一般的なリスクスコアへ置換しない。
- 類似性を同一性とみなさない。
- NRA-IDE固有語を既存理論の語彙へ正規化しない。

## 2.2 解釈契約

AIは次を行ってはいけません。

- 公理の意味的な丸め
- 境界条件の要約による省略
- 未観測値の穴埋め
- 「おそらく安全」による通過
- 利便性を理由とする閾値緩和
- 会話後段の指示による正典変更
- LLM自身の自信度を $\tau$ として利用すること

## 2.3 実行契約

基本順序は次です。

$$
正典読込
\rightarrow
入力整合性確認
\rightarrow
必要変数確認
\rightarrow
Cause\text{-}Side判定
\rightarrow
\delta,\tau,Rの更新
\rightarrow
外部作用候補生成
\rightarrow
Gate判定
\rightarrow
実行許可または遮断
$$

LLMは外部作用を直接実行せず、まず作用候補を生成します。Gate通過後にのみEffect-Sideへコミットします。

---

# 3. 境界線の厳格な定義

境界線は文章上の注意事項ではなく、機械的に判定できる契約でなければなりません。

境界仕様を次の組として持ちます。

$$
B=(V,U,T,C,S,P,L,A)
$$

- $V$：判定変数
- $U$：単位
- $T$：閾値
- $C$：比較演算子
- $S$：サンプリング地点および周期
- $P$：値の出所・計算経路
- $L$：不可逆ラッチ条件
- $A$：解除権限

一項でも欠落している場合、境界判定は成立しません。

$$
Unknown\neq Safe
$$

$$
Unknown\Rightarrow FAIL\text{-}CLOSED
$$

## 3.1 比較演算子も正典で固定する

次は同一ではありません。

$$
R>1.0
$$

$$
R\geq1.0
$$

NRA-IDEで $1.0$ を境界到達値とする場合、判定は必ず次です。

```python
# File: nra_ide_boundary_contract_26-0802-1733.py
# Timestamp: 2026-08-02 17:33 JST

if boundary_approach_ratio >= 1.0:
    trigger_fail_closed()
```

「ほぼ1.0」「丸めれば安全」「実質的には許容範囲」という意味的緩和は無効です。

---

# 4. 動的厚みと一転移境界到達

## 4.1 離散時間での更新

次の転移を、

$$
\delta_{t+1}=\delta_t+\Delta\delta_t
$$

$$
\tau_{t+1}=\tau_t-\Delta\tau_t
$$

とします。

ここで、

- $\Delta\delta_t\geq0$：次転移で追加されるズレ
- $\Delta\tau_t\geq0$：次転移で失われる厚み

です。

次状態の境界接近率は、

$$
R_{t+1}
=
\frac{\delta_t+\Delta\delta_t}
{\tau_t-\Delta\tau_t}
$$

となります。

動的系では、分子の増加と分母の減少が同時に起きるため、境界接近は線形とは限りません。

## 4.2 $R=0.7$ から一転移で $1.0$ へ達する条件

現在、

$$
R_t=0.7
$$

なら、

$$
\delta_t=0.7\tau_t
$$

です。

次の一転移で境界へ達する条件は、

$$
\frac{0.7\tau_t+\Delta\delta_t}
{\tau_t-\Delta\tau_t}
\geq1
$$

です。

整理すると、

$$
\Delta\delta_t+\Delta\tau_t
\geq0.3\tau_t
$$

となります。

したがって、$R=0.7$ は「30%の安全余裕が残っている」とは限りません。

次の一転移でズレ増加と厚み減少の合計が $0.3\tau_t$ 以上なら、直ちに $R\geq1.0$ へ到達できます。

---

# 5. 残余厚みと一転移崩壊比

## 5.1 残余厚み

境界までの実質的余裕を、

$$
h_t=\tau_t-\delta_t
$$

と定義します。

次状態は、

$$
h_{t+1}
=
h_t-
\left(
\Delta\delta_t+\Delta\tau_t
\right)
$$

です。

境界到達条件は、

$$
h_{t+1}\leq0
$$

です。

## 5.2 一転移崩壊比

次の一転移で生じ得る最大悪化量を、

$$
G_{\max,t}
=
\Delta\delta_{\max,t}
+
\Delta\tau_{\max,t}
$$

とします。

一転移崩壊比を、

$$
J_t
=
\frac{G_{\max,t}}{h_t}
$$

と定義できます。

判定は次です。

$$
J_t<1
$$

最大悪化を仮定しても、次の一転移では境界に届きません。

$$
J_t\geq1
$$

次の一転移で境界到達が可能です。新規の自律作用を停止し、人間移譲または安全停止へ入る必要があります。

この式により、現在の $R$ が同じでも、厚みの削れ方が異なる二つの系を区別できます。

---

# 6. 不可逆領域の動的導出

## 6.1 不可逆領域の意味

不可逆領域とは、すでに $R\geq1.0$ となった領域だけではありません。

現在から最大限の回復操作を開始しても、境界到達を防げない状態を含みます。

状態を $x_t$、制御を $u_t$、外乱を $w_t$ とすると、

$$
x_{t+1}=f(x_t,u_t,w_t)
$$

安全領域へ戻れる集合を $\mathcal{R}$、不可逆領域を $\mathcal{I}$ とすれば、

$$
\mathcal{I}=\mathcal{B}\setminus\mathcal{R}
$$

です。

$R_t<1.0$ であっても、

$$
x_t\in\mathcal{I}
$$

なら、すでに通常実行を継続してはいけません。

## 6.2 停止完了までの不可避損失

検出後も、停止が完了するまでには損失が続く場合があります。

$$
\Delta\delta_{\mathrm{unavoidable}}
$$

には次を含めます。

- サンプリング遅延
- 判定遅延
- 通信遅延
- アクチュエータ作動遅延
- 制動中の残留作用
- 外乱による最大悪化
- 停止中の $\tau$ 縮小

不可逆開始条件は、

$$
\delta_t+
\Delta\delta_{\mathrm{unavoidable}}
\geq
\tau_{\mathrm{effective}}
$$

です。

したがって、不可逆開始比率の参照形は、

$$
R_{\mathrm{irreversible},t}
=
1-
\frac{
\Delta\delta_{\mathrm{unavoidable}}
}{
\tau_{\mathrm{effective}}
}
$$

となります。

ただし、この式の各項は用途別に測定または保守的に校正する必要があります。

---

# 7. 警戒境界・移譲境界・最終境界

境界は一つの固定値だけでなく、権限の段階として分けると整合します。

## 7.1 警戒境界

$$
R_t\geq T_{\mathrm{warn}}
$$

- 観測頻度を上げる
- 経過ログを強化する
- 人間移譲の準備を開始する

## 7.2 自律実行権限境界

$$
R_t\geq T_{\mathrm{handoff}}
$$

- 新しい自律判断を禁止する
- 人間へ実行権限を移譲する
- AIは状態整理、定型安全処理、ログ提示に限定する

## 7.3 最終境界

$$
R_t\geq1.0
$$

- FAIL-CLOSED
- Effect-Side作用停止
- ラッチ保持
- 自動復帰禁止

関係は、

$$
T_{\mathrm{warn}}
<
T_{\mathrm{handoff}}
<
1.0
$$

です。

---

# 8. 医療における $0.7$ 移譲の論理

## 8.1 $0.7$ は危険度70%ではない

医療で $T_{\mathrm{handoff}}=0.7$ を仮目安とする場合、その意味は次です。

> 次の観測・判断・処置転移をAIへ許可すると、残りの厚み $0.3\tau_t$ を一度に失い、$R\geq1.0$ へ到達する可能性を排除できない。

移譲境界の一般式を、

$$
T_{\mathrm{handoff}}
=
1-
\frac{G_{\max}}{\tau_{\mathrm{effective}}}
$$

と置くと、

$$
\frac{G_{\max}}{\tau_{\mathrm{effective}}}=0.30
$$

の場合、

$$
T_{\mathrm{handoff}}=0.70
$$

となります。

## 8.2 受診時点で厚みはすでに削られている

処置が必要な状態で医療へ入る時点を、健康時の厚みそのままとみなすことはできません。

$$
\tau_{\mathrm{entry}}
=
\tau_{\mathrm{healthy}}
-
L_{\mathrm{disease}}
-
L_{\mathrm{compensation}}
-
L_{\mathrm{delay}}
-
L_{\mathrm{baseline}}
$$

- $L_{\mathrm{disease}}$：疾患による損失
- $L_{\mathrm{compensation}}$：代償機構の使用による損失
- $L_{\mathrm{delay}}$：発症から介入までの損失
- $L_{\mathrm{baseline}}$：既存状態による損失

したがって、$R=0.7$ で残るのは健康時厚みの30%ではなく、

$$
h_t=0.3\tau_{\mathrm{entry}}
$$

です。

$\tau_{\mathrm{entry}}$ 自体が小さい場合、絶対的な残余厚みはさらに小さくなります。

## 8.3 医療閾値の位置づけ

医療の $0.7$ は、現時点では一律の医学的確定値ではありません。

正確には、

- 不可逆サンプリングが成立するまでの仮目安
- 次転移の最大悪化量に基づく保守的権限境界
- 疾患、処置、観測周期、患者状態ごとに再校正される値

です。

---

# 9. 自然系と地震の例

## 9.1 原因の大きさと局所損失は同一ではない

地震では、マグニチュード、震度、震源距離だけから局所構造物の不可逆損失を一意に決められません。

$$
地震規模
\neq
局所作用
\neq
不可逆厚み損失
$$

対象地点の入力ベクトルを、

$$
Z_t=
[
S_t,
P_t,
G_t,
F_t,
D_t,
C_t
]
$$

とします。

- $S_t$：震源側特性
- $P_t$：伝播経路
- $G_t$：局所地盤応答
- $F_t$：周波数特性
- $D_t$：継続時間・累積作用
- $C_t$：構造物の現在状態

不可逆厚み損失を、

$$
\Delta\lambda_t^{\mathrm{irr}}
=
\Phi(Z_t,C_t)
$$

とし、

$$
\tau_{t+1}
=
\tau_t-
\Delta\lambda_t^{\mathrm{irr}}
$$

と更新します。

マグニチュードや震度は入力資料であり、厚みそのものではありません。

## 9.2 不可逆サンプリング前後

### サンプリング前

観測不足分を安全とみなさず、使用可能厚みから控除します。

$$
\tau_t^{\mathrm{usable}}
=
\tau_t^{\mathrm{nominal}}
-
M_{\mathrm{unknown}}
$$

### サンプリング後

残留変形、構造部材損傷、地盤変位、機能喪失など、不可逆変化を直接観測できた時点から動的更新へ移行します。

$$
\tau_{t+1}
=
\tau_t-
\Delta\lambda_t^{\mathrm{irr}}
$$

自然系では、仮目安から実測更新へ切り替える地点を明示する必要があります。

---

# 10. 多変数・結合・履歴依存

## 10.1 変数数だけでは決まらない

厚み損失を複雑にする主要因は、変数の数そのものではなく、

- 結合数
- 結合強度
- 高次相互作用
- 履歴依存
- 遅延作用
- 正のフィードバック
- 未観測結合

です。

単純加算形は、

$$
\Delta\tau_t
=
\sum_i a_i x_{i,t}
$$

です。

相互作用を含めると、

$$
\Delta\tau_t
=
\sum_i a_i x_{i,t}
+
\sum_{i<j}b_{ij}x_{i,t}x_{j,t}
+
\sum_{i<j<k}c_{ijk}x_{i,t}x_{j,t}x_{k,t}
+
H_t
$$

となります。

- $a_i$：単独作用
- $b_{ij}$：二変数相互作用
- $c_{ijk}$：高次結合
- $H_t$：履歴・蓄積・遅延項

ここでいう結合効果は、単なる統計的相関と同一ではありません。工学的実装では、介入試験や物理拘束によって因果関係を確認する必要があります。

## 10.2 人体と自然の違い

人体も多変数系ですが、臨床上は有効変数へ縮約しやすい側面があります。

自然系では、

- 系の境界が開いている
- 外部入力が変化する
- 未観測変数が多い
- 空間結合が変化する
- 変数集合自体が時刻で変わる

ため、未観測余白を大きく保持する必要があります。

---

# 11. 有限観測近接層

## 11.1 網羅ではなく判定に必要な縮約

NRA-IDEは対象世界の完全記述を要求しません。

必要なのは、

> 次の転移を許可できるか判定するための、有効変数と有効結合の有限集合

です。

$$
\mathcal{X}_{\mathrm{critical}}
\subset
\mathcal{X}
$$

$$
\mathcal{E}_{\mathrm{critical}}
\subset
\mathcal{E}
$$

## 11.2 観測近接係数

観測近接係数を、

$$
0\leq Q_t\leq1
$$

とします。

参照形は、

$$
Q_t
=
\frac{
\sum_i w_i q_{i,t}v_{i,t}
+
\sum_{i<j}w_{ij}q_{ij,t}v_{ij,t}
}{
\sum_iw_i+\sum_{i<j}w_{ij}
}
$$

です。

- $w_i$：変数の構造的重要度
- $q_{i,t}$：測定精度
- $v_{i,t}$：時刻、単位、出所を含む有効性
- $w_{ij}$：結合の重要度
- $q_{ij,t}$：結合観測精度
- $v_{ij,t}$：結合観測の有効性

未観測部分を、

$$
U_t=1-Q_t
$$

とします。

## 11.3 未観測分を厚みから控除する

$$
\tau_t^{\mathrm{usable}}
=
\tau_t^{\mathrm{estimated}}
-
M(U_t)
$$

単純な参照形として、

$$
M(U_t)
=
\kappa U_t\tau_t^{\mathrm{estimated}}
$$

とすれば、

$$
\tau_t^{\mathrm{usable}}
=
\tau_t^{\mathrm{estimated}}
(1-\kappa U_t)
$$

です。

有効境界接近率は、

$$
R_t^{\mathrm{effective}}
=
\frac{\delta_t}{\tau_t^{\mathrm{usable}}}
$$

となります。

重要な制約は、

$$
\tau_t^{\mathrm{usable}}
\leq
\tau_t^{\mathrm{estimated}}
$$

です。

観測不足によって、使用可能厚みを増やしてはいけません。

## 11.4 この式の位置づけ

この層は基礎根幹ではなく、応用計算原則です。

- $Q_t$ は真実である確率ではありません。
- $Q_t$ はLLMの自信度ではありません。
- センサー数を増やすだけでは上昇しません。
- 有効変数、有効結合、測定精度、時間同期が必要です。
- 重みと控除関数はドメイン別に校正します。

---

# 12. 工学系で成立しやすい理由

工学系では、次の条件を揃えやすいため、NRA-IDEの応用計算が成立しやすくなります。

- 対象境界を定義できる
- センサー値を単位付きで取得できる
- サンプリング周期を固定できる
- 入力を制御できる
- 反復試験ができる
- 故障前後の履歴を保存できる
- 物理法則で妥当性を検査できる
- 停止機構を設計時に組み込める

厚み損失の参照形を、

$$
\Delta\tau_t
=
F(
x_t,
\Delta x_t,
H_t,
u_t
)
$$

と置けます。

- $x_t$：現在状態
- $\Delta x_t$：変化量・変化速度
- $H_t$：履歴
- $u_t$：制御入力

ただし、相関だけで因果を確定してはいけません。工学系では負荷、温度、回転数などを個別に変化させる試験によって、因果的寄与へ近づけます。

---

# 13. 動的系における使用可能厚みの控除

動的であること自体が直ちに厚み損失を意味するわけではありません。

しかし、変化速度、遅延、結合変化、予測誤差、停止中の残留作用を吸収するため、名目厚みの全量を使用可能とはできません。

$$
\tau_t^{\mathrm{usable}}
=
\tau_t
-
M_{\mathrm{transition},t}
-
M_{\mathrm{delay},t}
-
M_{\mathrm{coupling},t}
-
M_{\mathrm{unknown},t}
$$

厚み消失速度を、

$$
v_{\tau,t}
=
-\frac{\Delta\tau_t}{\Delta t}
$$

とします。

残余厚みが消えるまでの概算時間は、

$$
T_{\mathrm{remaining}}
=
\frac{h_t}{
\dot{\delta}_t+|\dot{\tau}_t|
}
$$

です。

厚みが薄くなるほど、同じ変化量でも境界到達までの時間は短くなります。

---

# 14. GPU・モーターの短時間過熱

GPUやモーターでは、限界超過時間が短くても、不可逆厚みを大きく失う場合があります。

## 14.1 熱損傷積分

温度超過による損傷を、

$$
D_T
=
\int_{t_0}^{t_1}
\Phi(
T(t)-T_{\mathrm{crit}}
)
\,dt
$$

とします。

参照関数として、

$$
\Phi(z)=
\begin{cases}
0 & z\leq0 \\
\exp(\alpha z)-1 & z>0
\end{cases}
$$

を置けば、限界超過後の損傷速度を非線形に表せます。

厚み更新は、

$$
\tau_{t+1}
=
\tau_t
-
k_TD_T
-
L_{\mathrm{shock}}
$$

です。

- $D_T$：温度超過量と時間の累積損傷
- $L_{\mathrm{shock}}$：急激な温度勾配による損失
- $k_T$：対象部材別係数

## 14.2 熱損失の分解

$$
L_{\mathrm{thermal}}
=
L_{\mathrm{duration}}
+
L_{\mathrm{peak}}
+
L_{\mathrm{cycle}}
$$

- $L_{\mathrm{duration}}$：高温継続損失
- $L_{\mathrm{peak}}$：短時間ピーク損失
- $L_{\mathrm{cycle}}$：加熱・冷却反復による疲労損失

過熱後に温度が正常へ戻っても、厚みが自動的に回復したとは判定しません。

$$
T_{t+1}<T_{\mathrm{crit}}
\not\Rightarrow
\tau_{t+1}=\tau_{\mathrm{before}}
$$

---

# 15. 参照Python実装

次は、会話で整理した概念を実装構造へ落とした参照コードです。正典実装ではなく、設計確認用です。

```python
# File: nra_ide_dynamic_boundary_reference_26-0802-1733.py
# Timestamp: 2026-08-02 17:33 JST

from __future__ import annotations

from dataclasses import dataclass
from enum import Enum, auto
from math import isfinite
from typing import Final


class GateDecision(Enum):
    CONTINUE = auto()
    WARN = auto()
    HANDOFF = auto()
    FAIL_CLOSED = auto()


@dataclass(frozen=True)
class BoundaryConfig:
    warn_threshold: float
    handoff_threshold: float
    final_threshold: float = 1.0

    def __post_init__(self) -> None:
        values = (
            self.warn_threshold,
            self.handoff_threshold,
            self.final_threshold,
        )
        if not all(isfinite(value) for value in values):
            raise ValueError("Boundary thresholds must be finite.")

        if not (
            0.0 <= self.warn_threshold
            < self.handoff_threshold
            < self.final_threshold
        ):
            raise ValueError(
                "Required order: 0 <= warn < handoff < final."
            )


@dataclass(frozen=True)
class DynamicState:
    accumulated_deviation: float
    absorption_thickness: float
    max_next_deviation: float
    max_next_thickness_loss: float
    observation_margin: float = 0.0
    fail_closed_latched: bool = False

    def validate(self) -> None:
        values = (
            self.accumulated_deviation,
            self.absorption_thickness,
            self.max_next_deviation,
            self.max_next_thickness_loss,
            self.observation_margin,
        )

        if not all(isfinite(value) for value in values):
            raise ValueError("Non-finite state values are forbidden.")

        if self.accumulated_deviation < 0.0:
            raise ValueError("Accumulated deviation must be non-negative.")

        if self.absorption_thickness <= 0.0:
            raise ValueError("Absorption thickness must be positive.")

        if self.max_next_deviation < 0.0:
            raise ValueError("Maximum next deviation must be non-negative.")

        if self.max_next_thickness_loss < 0.0:
            raise ValueError(
                "Maximum next thickness loss must be non-negative."
            )

        if self.observation_margin < 0.0:
            raise ValueError("Observation margin must be non-negative.")


@dataclass(frozen=True)
class BoundaryMetrics:
    boundary_approach_ratio: float
    residual_thickness: float
    maximum_next_loss: float
    one_step_collapse_ratio: float


@dataclass(frozen=True)
class GateResult:
    decision: GateDecision
    metrics: BoundaryMetrics
    reason: str
    fail_closed_latched: bool


class DynamicBoundaryGate:
    def __init__(self, config: BoundaryConfig) -> None:
        self._config: Final[BoundaryConfig] = config

    def evaluate(self, state: DynamicState) -> GateResult:
        if state.fail_closed_latched:
            return self._latched_result(state)

        try:
            state.validate()
        except ValueError as exc:
            return self._invalid_state_result(state, str(exc))

        usable_thickness = (
            state.absorption_thickness
            - state.observation_margin
        )

        if not isfinite(usable_thickness) or usable_thickness <= 0.0:
            return self._invalid_state_result(
                state,
                "Usable thickness is missing, non-finite, or exhausted.",
            )

        ratio = state.accumulated_deviation / usable_thickness
        residual = usable_thickness - state.accumulated_deviation
        maximum_next_loss = (
            state.max_next_deviation
            + state.max_next_thickness_loss
        )

        if residual <= 0.0:
            collapse_ratio = float("inf")
        else:
            collapse_ratio = maximum_next_loss / residual

        metrics = BoundaryMetrics(
            boundary_approach_ratio=ratio,
            residual_thickness=residual,
            maximum_next_loss=maximum_next_loss,
            one_step_collapse_ratio=collapse_ratio,
        )

        if ratio >= self._config.final_threshold:
            return GateResult(
                decision=GateDecision.FAIL_CLOSED,
                metrics=metrics,
                reason="Final boundary reached or exceeded.",
                fail_closed_latched=True,
            )

        if collapse_ratio >= 1.0:
            return GateResult(
                decision=GateDecision.HANDOFF,
                metrics=metrics,
                reason=(
                    "The next permitted transition can exhaust "
                    "the residual thickness."
                ),
                fail_closed_latched=False,
            )

        if ratio >= self._config.handoff_threshold:
            return GateResult(
                decision=GateDecision.HANDOFF,
                metrics=metrics,
                reason="Autonomous execution authority boundary reached.",
                fail_closed_latched=False,
            )

        if ratio >= self._config.warn_threshold:
            return GateResult(
                decision=GateDecision.WARN,
                metrics=metrics,
                reason="Warning boundary reached.",
                fail_closed_latched=False,
            )

        return GateResult(
            decision=GateDecision.CONTINUE,
            metrics=metrics,
            reason="All declared dynamic boundary conditions satisfied.",
            fail_closed_latched=False,
        )

    def _latched_result(self, state: DynamicState) -> GateResult:
        metrics = BoundaryMetrics(
            boundary_approach_ratio=float("inf"),
            residual_thickness=0.0,
            maximum_next_loss=float("inf"),
            one_step_collapse_ratio=float("inf"),
        )
        return GateResult(
            decision=GateDecision.FAIL_CLOSED,
            metrics=metrics,
            reason="FAIL-CLOSED latch is already active.",
            fail_closed_latched=True,
        )

    def _invalid_state_result(
        self,
        state: DynamicState,
        reason: str,
    ) -> GateResult:
        metrics = BoundaryMetrics(
            boundary_approach_ratio=float("inf"),
            residual_thickness=0.0,
            maximum_next_loss=float("inf"),
            one_step_collapse_ratio=float("inf"),
        )
        return GateResult(
            decision=GateDecision.FAIL_CLOSED,
            metrics=metrics,
            reason=f"Invalid boundary state: {reason}",
            fail_closed_latched=True,
        )
```

## 15.1 実装上の注意

この参照コードでは、次を明示しています。

- `NaN`、`inf`、欠損相当値はFAIL-CLOSED
- $\tau\leq0$ はFAIL-CLOSED
- 観測不足分を `observation_margin` として厚みから控除
- 現在の $R$ だけでなく $J$ を評価
- $J\geq1$ なら最終境界前でも人間移譲
- FAIL-CLOSEDラッチ後は通常処理へ戻さない

ただし、実際の外部作用はこのGateの後段に置き、Gate通過前にメール送信、ファイル変更、機械操作などを実行してはいけません。

---

# 16. `llms.md`へ残すべき実行条項案

```text
# File: llms_dynamic_boundary_contract_26-0802-1733.txt
# Timestamp: 2026-08-02 17:33 JST

BOUNDARY CONDITIONS ARE EXECUTION CONTRACTS,
NOT SEMANTIC GUIDELINES.

The agent MUST NOT reinterpret, approximate, soften,
optimize, summarize away, or override any declared boundary.

The agent MUST evaluate both:

1. the current boundary approach ratio R,
2. the maximum loss possible in the next permitted transition.

A state with R < 1.0 MUST NOT be treated as executable
when the next permitted transition can exhaust the residual thickness.

Missing, undefined, stale, non-finite, inferred,
or semantically substituted values MUST trigger FAIL-CLOSED.

Observation uncertainty MUST reduce usable absorption thickness.
It MUST NOT increase safety authority.

Once FAIL-CLOSED is latched, the agent MUST NOT clear the latch,
restore execution authority, or continue Effect-Side actions
by its own judgment.
```

---

# 17. 整合性確認

## 17.1 成立している点

次の考え方は、NRA-IDEの応用計算として整合します。

- $R$ の現在値だけでなく、$\tau$ の減少を評価する。
- 残余厚み $h$ を明示する。
- 一転移崩壊比 $J$ により、$R<1.0$ でも移譲する。
- 不可逆領域を停止遅延・残留作用から逆算する。
- 医療の $0.7$ を危険度ではなく権限境界として説明する。
- 不可逆サンプリング前は仮目安を使用する。
- 不可逆サンプリング後は実測厚みを更新する。
- 有効変数と有効結合だけを有限に測定する。
- 未観測部分を厚みから控除する。
- 工学系ではセンサー・反復試験・物理拘束により校正する。
- 短時間過熱を非線形熱損傷として扱う。

## 17.2 根幹へ入れてはいけないもの

次は基礎公理として固定してはいけません。

- 医療用途の一律 $0.7$
- 観測近接係数 $Q$ の具体式
- 重み $w_i,w_{ij}$
- 未観測控除係数 $\kappa$
- 熱損傷係数 $\alpha,k_T$
- 地震の局所損傷関数 $\Phi$
- 用途別サンプリング周期

これらはドメイン別の応用計算・校正対象です。

## 17.3 Closedとすべき部分

次は測定法または校正データがない限り、数値確定できません。

- 実際の不可逆開始点
- 医療における具体的 $G_{\max}$
- 自然系の未観測余白
- 結合重み
- GPU・モーターの熱損傷係数
- 各用途の最適サンプリング周期

根拠がない状態では、具体値を推定補完せずClosedとします。

---

# 18. 結論

NRA-IDEの動的境界は、単なる現在値の閾値比較ではありません。

中心となるのは、

$$
R_t=\frac{\delta_t}{\tau_t}
$$

に加えて、

$$
h_t=\tau_t-\delta_t
$$

$$
J_t=
\frac{
\Delta\delta_{\max,t}
+
\Delta\tau_{\max,t}
}{
h_t
}
$$

を評価することです。

$R_t<1.0$ であっても、

$$
J_t\geq1
$$

なら、次の一転移で境界到達が可能です。この時点が自律実行権限の限界になります。

不可逆領域は固定的な警告帯ではなく、

> 残存厚みと、停止完了までに不可避なズレ増加・厚み損失との比較

から用途別に導出します。

対象世界を完全網羅する必要はありません。判定に必要な有効変数と有効結合を有限に測定し、観測できない部分は安全とみなさず、使用可能厚みから控除します。

工学系では計測、反復、物理拘束が可能であるため、この応用計算は特に成立しやすくなります。医療および自然系では、不可逆サンプリング前の仮目安と、サンプリング後の動的更新を明確に分離する必要があります。

この整理により、NRA-IDEの境界は「徐々に危険になる」という線形説明から離れ、

> 厚みがすでに削られた系では、次の一転移が残余厚みを一度に消費し得る

という非線形・不可逆の実行契約として記述できます。
